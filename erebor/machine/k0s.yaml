apiVersion: k0s.k0sproject.io/v1beta1
kind: ClusterConfig
metadata:
  name: erebor
spec:
  api:
    sans:
      - erebor
      - erebor.test
  network:
    provider: custom
    podCIDR: 10.244.0.0/16
    serviceCIDR: 10.96.0.0/12
    kubeProxy:
      disabled: false
  storage:
    type: etcd
  telemetry:
    enabled: false
