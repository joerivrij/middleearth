all:
  children:
    container_machines:
      hosts:
        erebor:
          ansible_host: erebor.test
          ansible_user: "{{ lookup('env', 'USER') }}"
          ansible_python_interpreter: /usr/bin/python3
