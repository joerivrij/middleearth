apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: rook-ceph
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`rook-ceph.erebor.test`)
      kind: Rule
      services:
        - name: rook-ceph-mgr-dashboard
          port: 7000
