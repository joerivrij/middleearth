apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: rook-ceph
resources:
  - namespace.yaml
  - repository.yaml
  - release.yaml
configMapGenerator:
  - name: rook-ceph-operator-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
