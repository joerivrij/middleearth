# Applications

This directory is reserved for applications owned by the platform itself.
Lab workloads should normally remain in their lab repository and be connected
from a cluster definition under `clusters/`.

