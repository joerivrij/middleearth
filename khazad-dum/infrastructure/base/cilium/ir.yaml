apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: hubble
  namespace: cilium-system
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`hubble.khazad-dum.lotr`)
      services:
        - name: hubble-ui
          port: 80
